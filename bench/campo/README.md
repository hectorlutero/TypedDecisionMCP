# Campo — log de economia Cursor

1. Segue `docs/roteiros-economia-campo.md` (R1–R8).
2. Copia o molde: cada corrida = uma linha JSON em `log.jsonl`.
3. No fim da semana: `npx tsx bench/campo/summarize.ts` (se existir) ou agrega à mão na secção **Próximos passos (campo)** dos docs.

`log.jsonl` pode ficar local (não é obrigatório commitar números pessoais).
