---
description: Call decide for typed local ifs
alwaysApply: true
---

When the hop is one of command, subagent, diff, file, commit: call MCP tool `decide`. Do not write the probability in prose. Read `action`: `auto` continue, `review` ask, `stop` do not execute.
